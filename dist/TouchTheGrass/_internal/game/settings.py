import os
import pygame

# Screen settings
SCREEN_SIZE = (800, 600)
CENTER = (SCREEN_SIZE[0] // 2, SCREEN_SIZE[1] // 2)

# Scale limits
MIN_SCALE = 0.9
MAX_SCALE = 1.2

# Asset paths are now defined in paths.py
